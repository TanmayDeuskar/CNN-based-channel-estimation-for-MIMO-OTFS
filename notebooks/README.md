# Notebook

The original notebook/code dump was used as the source for this cleaned repository.

The cleaned implementation is intentionally script-based so that training, evaluation, SNR sweeps, and benchmarking can be run independently.
